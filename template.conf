<VirtualHost *:80>
    ServerName 206.189.226.167/${AppName}
    WSGIScriptAlias / /var/www/${AppName}/${AppName}.wsgi
    <Directory /var/www/${AppName}/${AppName}/>
        Order allow,deny
        Allow from all
    </Directory>
    Alias /static /var/www/${AppName}/${AppName}/static
    <Directory /var/www/${AppName}/${AppName}/static/>
        Order allow,deny
        Allow from all
    </Directory>
</VirtualHost>
